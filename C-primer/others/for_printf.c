#include <stdio.h>
int main() {
    int num = 0;
    for (printf("keep entering nums!\n"); num != 520;) {
      scanf("%d", &num);
    }
    printf("thats the num i want!\n");

    //getchar();
    system("pause");
    return 0;
}
